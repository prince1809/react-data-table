#!/bin/bash

set -e

PROJECT_DIRECTORY = "react-data-table"
SITE_DIRECTORY = "$PROJECT_DIRECTORY-site"
GITHUB_REPO = "github.com/prince1809"

# Move to parent dir
cd ../

#setup repo if it doesn't exist
